#up
  %a{:href => "#"}
    %img{:src => "/up.png"}
#down
  %a{:href => "#"}
    %img{:src => "/down.png"}

#items
  - @items.each do |item|
    .item
      %a{:href => item.uri}
        %img{:src => item.image(:thumb)}
        .label= item.title
      

  .spacer